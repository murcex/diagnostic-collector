-- (1) Replace <sensorReadOnlyLoginHere> with your proper login
-- (2) Convert to .sql
-- (3) Remove these headers and uncomment cmds below
-- Check and Create Database Login
--DECLARE @check INT

--SELECT @check = COUNT(*) FROM [sys].[database_principals] WHERE [type] = 'S' AND [name] = N'<sensorReadOnlyLoginHere>'

--IF @check = 0
--BEGIN
  --CREATE USER [<sensorReadOnlyLoginHere>]
  --FOR LOGIN [<sensorReadOnlyLoginHere>]
--END

-- Grant Login to Objects
--GRANT SELECT ON [dbo].[vw_DNS_Article] TO [<sensorReadOnlyLoginHere>]
